# JSON Parser

Implenting a JSON parser for learning.

Run the file `JsonParsingDemo.java` to see a demo.

Implementation of JSON parsing with following limitations:
  - String's cannot have quotes (") within them.
  - Numbers can be only in decimal format.
